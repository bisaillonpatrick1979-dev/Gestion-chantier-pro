# Moon Tide background asset

Place your background image here with this exact filename:

- `moon-tide-bg.jpg`

Expected path used by the app:

- `/themes/moon-tide/moon-tide-bg.jpg`

If the image is missing, the theme keeps a gradient fallback background.
