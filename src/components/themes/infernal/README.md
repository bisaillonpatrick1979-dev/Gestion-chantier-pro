# Infernal theme notes

This folder is reserved for the future full Infernal visual components.
Current Infernal overrides are injected from `src/components/ThemeInjector.tsx` so they win after the active theme CSS.
