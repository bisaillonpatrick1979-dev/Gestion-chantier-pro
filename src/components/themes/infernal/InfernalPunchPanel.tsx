'use client'

import type { ReactNode } from 'react'

export default function InfernalPunchPanel({ children }: { children: ReactNode }) {
  return <section className="infernal-punch-panel">{children}</section>
}
