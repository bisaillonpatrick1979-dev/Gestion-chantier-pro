'use client'

import type { ReactNode } from 'react'

export default function InfernalPunchButton({ children }: { children: ReactNode }) {
  return <div className="infernal-punch-button-wrap">{children}</div>
}
