import type { ReactNode } from 'react'

export default function InfernalTransparentCard({ children }: { children: ReactNode }) {
  return <div className="infernal-transparent-card">{children}</div>
}
